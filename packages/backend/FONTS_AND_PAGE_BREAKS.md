# Шрифты и Page Breaks в PDF

## 🎨 Шрифты

### Gilroy SemiBold - для заголовков

Используется для:

- `h1, h2, h3, h4, h5, h6` - HTML заголовки
- `.heading-1, .heading-2, .heading-3, .heading-4` - классы заголовков
- `.section-title` - заголовки секций

**Веса:**

- Semibold (600) - для h2, h3, .section-title
- Bold (700) - для h1

### Inter - для остального текста

Используется для:

- Body text (обычный текст)
- Параграфы
- Таблицы
- Labels и badge

**Веса:**

- Regular (400) - обычный текст
- Medium (500) - выделенный текст
- SemiBold (600) - более выделенный
- Bold (700) - жирный текст

### CSS переменные

```css
--font-family-body:
  "Inter", ...fallbacks --font-family-heading: "Gilroy", ...fallbacks;
```

## 📄 Page Breaks

### Автоматическая защита

Следующие элементы **НЕ разрываются внутри**:

- `.section` - секции
- `.info-block` - информационные блоки
- `h1-h6` - заголовки
- `tbody tr` - строки таблиц

### Классы для управления

#### `.keep-together`

Держит весь блок вместе (не разрывает внутри).

**Использование:**

```html
<div class="keep-together">
  <table>
    ...
  </table>
  <div class="summary">...</div>
</div>
```

**Где применено:**

- Таблица услуг + Summary
- Секция "История платежей"

#### `.avoid-break-before`

Избегает разрыва страницы ПЕРЕД элементом.

**Где применено:**

- Footer

#### `.page-break-before`

ПРИНУДИТЕЛЬНО создает новую страницу.

**Использование:**

```html
<section class="page-break-before">
  <h2>Новый раздел на новой странице</h2>
</section>
```

### Настройки Puppeteer

Margins в `pdf.service.ts`:

```typescript
margin: {
  top: "5mm",
  right: "5mm",
  bottom: "5mm",
  left: "5mm",
}
```

### Best Practices

1. ✅ **Группируйте связанный контент** в `.keep-together`
2. ✅ **Заголовки автоматически** держатся со следующим контентом
3. ✅ **Таблицы** - строки не разрываются (tbody tr)
4. ⚠️ **Не злоупотребляйте** page-break классами

### Тестирование

1. Проверьте с разным количеством данных
2. Проверьте длинные таблицы (10+ строк)
3. Проверьте с комментариями и без
4. Проверьте с историей платежей и без

## 🔍 Отладка

### Проблема: Разрывы все равно происходят

**Причины:**

1. Контент больше одной страницы
2. Вложенный flex/grid контейнер
3. Конфликт CSS правил

**Решение:**

1. Уменьшите margins в `pdf.service.ts`
2. Используйте `!important` в CSS
3. Проверьте Chrome DevTools с Print preview

### Проблема: Шрифты не загружаются

**Причины:**

1. CDN недоступен
2. Неправильный путь к шрифтам

**Решение:**

1. Проверьте интернет соединение
2. Используйте fallback шрифты (уже настроено)
3. В продакшн: используйте локальные шрифты

## 📦 Production

На Ubuntu сервере:

```bash
# Установить зависимости Puppeteer
sudo apt-get install -y \
  fonts-liberation \
  libappindicator3-1 \
  libnss3 \
  libatk1.0-0
```

Puppeteer автоматически установит Chrome при `pnpm install`.

## 🎯 Итого

✅ Gilroy Semibold - заголовки  
✅ Inter - текст  
✅ Page breaks с `!important`  
✅ `.keep-together` для группировки  
✅ Автоматическая защита заголовков и строк таблиц
