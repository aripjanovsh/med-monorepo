import { Metadata } from "next";

export const metadata: Metadata = {
  title: "MaxMed - Settings",
  description: "Settings page",
};

export default function SettingsPage() {
  return <div></div>;
}
