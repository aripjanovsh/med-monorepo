# Analysis Form Builder - Индекс документации

Полная документация для модуля работы с шаблонами лабораторных анализов.

---

## 📚 Документация

### [README.md](./README.md)

**Главная документация модуля**

- Обзор возможностей
- Три режима работы (Editor, Interactive, View)
- Типы данных (AnalysisTemplate, FilledAnalysisData)
- Референсные диапазоны
- Примеры использования компонентов
- Утилиты и хелперы
- Структура модуля

---

### [JSON_GUIDE.md](./JSON_GUIDE.md)

**Руководство по созданию JSON структур**

- Базовая структура AnalysisTemplate
- Секции (AnalysisSection)
- Параметры (AnalysisParameter):
  - NUMBER - числовые показатели
  - TEXT - текстовые описания
  - BOOLEAN - бинарные результаты
- Референсные диапазоны (ReferenceRanges)
- Группы: men, women, children, default
- Полные примеры:
  - Общий анализ крови (ОАК)
  - Биохимический анализ крови
- Валидационные правила
- Советы и частые ошибки

---

### [USAGE_EXAMPLE.md](./USAGE_EXAMPLE.md)

**Примеры использования в коде**

7 практических примеров:

1. Создание шаблона анализа
2. Заполнение результатов анализа
3. Просмотр результатов анализа
4. Интерактивная форма для тестирования
5. Копирование из предыдущего анализа
6. Печать результатов
7. Сравнение результатов

---

## 🚀 Быстрый старт

### Для создания шаблона анализа

1. Прочитайте [README.md](./README.md) - раздел "Editor"
2. Изучите [JSON_GUIDE.md](./JSON_GUIDE.md) - структуру данных
3. Посмотрите примеры в [JSON_GUIDE.md](./JSON_GUIDE.md) - раздел "Полные примеры"

### Для заполнения результатов

1. Прочитайте [README.md](./README.md) - раздел "Interactive"
2. Смотрите [USAGE_EXAMPLE.md](./USAGE_EXAMPLE.md) - Пример 2

### Для просмотра результатов

1. Прочитайте [README.md](./README.md) - раздел "View"
2. Смотрите [USAGE_EXAMPLE.md](./USAGE_EXAMPLE.md) - Пример 3

---

## 🔍 Что искать в документации

| Вопрос                             | Документ         | Раздел                |
| ---------------------------------- | ---------------- | --------------------- |
| Как создать JSON шаблона вручную?  | JSON_GUIDE.md    | Типы параметров       |
| Как работают референсные значения? | JSON_GUIDE.md    | Референсные диапазоны |
| Как использовать компоненты?       | README.md        | Три режима работы     |
| Примеры реального кода?            | USAGE_EXAMPLE.md | Все примеры           |
| Как вычисляются статусы?           | README.md        | Вычисление статуса    |
| Структура FilledAnalysisData?      | JSON_GUIDE.md    | FilledAnalysisData    |
| Как хранить в БД?                  | USAGE_EXAMPLE.md | Структура данных в БД |
| Миграция со старого формата?       | README.md        | Формат данных         |

---

## 📦 Экспорты модуля

```typescript
// Компоненты
import {
  AnalysisFormEditor,
  AnalysisFormInteractive,
  AnalysisFormView,
} from "@/features/analysis-form-builder";

// Типы
import type {
  AnalysisTemplate,
  AnalysisSection,
  AnalysisParameter,
  FilledAnalysisData,
  SavedAnalysisData,
  ReferenceRanges,
  ReferenceRange,
  PatientGender,
} from "@/features/analysis-form-builder";

// Утилиты
import {
  createEmptyAnalysisTemplate,
  createNewParameter,
  createNewSection,
  normalizeAnalysisTemplate,
  formatReferenceRange,
  getApplicableRange,
  getReferenceStatus,
} from "@/features/analysis-form-builder";
```

---

## 🎯 Использование в проекте

### analysis-template feature

- **Editor** - создание шаблонов анализов
- Хранение: `AnalysisTemplate.parameters` (JSON string)

### service-order feature

- **Interactive** - заполнение результатов анализа
- **View** - просмотр результатов с индикаторами
- Хранение: `ServiceOrder.resultData` (JSON: SavedAnalysisData)

---

## 🎨 Особенности отображения

### Статусы значений

Система автоматически вычисляет и показывает статус каждого параметра:

- **NORMAL** 🟢 - значение в пределах нормы
- **HIGH** 🔴 - значение выше нормы
- **LOW** 🟡 - значение ниже нормы
- **UNKNOWN** ⚪ - нет референсных значений

### Логика выбора референсного диапазона

1. Если возраст < 18 → используется `children`
2. Если пол = MALE → используется `men`
3. Если пол = FEMALE → используется `women`
4. Fallback на `default`
5. Если нет подходящего → первый доступный

### Форматирование

- **NUMBER**: форматируется с разделителями тысяч + единица
- **TEXT**: отображается как есть
- **BOOLEAN**: true → "Положительно", false → "Отрицательно"

---

## 🆚 Сравнение с form-builder

| Характеристика            | form-builder       | analysis-form-builder     |
| ------------------------- | ------------------ | ------------------------- |
| Назначение                | Произвольные формы | Лабораторные анализы      |
| Типы полей                | 9 типов            | 3 типа (NUMBER/TEXT/BOOL) |
| Референсные значения      | ❌                 | ✅                        |
| Статусы (NORMAL/HIGH/LOW) | ❌                 | ✅                        |
| Условная видимость        | ✅                 | ❌                        |
| Единицы измерения         | ❌                 | ✅                        |
| Секции                    | ✅                 | ✅                        |

**Когда использовать analysis-form-builder:**

- ✅ Общий анализ крови
- ✅ Биохимический анализ
- ✅ Анализ мочи
- ✅ Гормональные исследования
- ✅ Любые результаты с числовыми показателями

**Когда использовать form-builder:**

- ✅ Протоколы осмотра
- ✅ Анкеты пациентов
- ✅ Произвольные медицинские формы

---

## 📝 Структура SavedAnalysisData

```typescript
type SavedAnalysisData = {
  // Идентификация шаблона
  templateId: string;
  templateName: string;

  // Полный шаблон (для истории)
  templateContent: AnalysisTemplate;

  // Заполненные данные
  filledData: FilledAnalysisData;

  // Метаданные
  metadata: {
    filledAt: string; // ISO timestamp
    patientId: string;
    serviceOrderId?: string;
  };
};
```

**Зачем хранить templateContent:**

- Шаблон может быть изменен или удален
- Нужна полная история для медицинской документации
- Возможность переиспользовать в других визитах

---

## 🛠 Правила разработки

### При добавлении новых типов параметров

1. Добавить тип в `AnalysisParameterType`
2. Обновить `AnalysisParameter` type
3. Добавить компонент в `components/internal/`
4. Обновить логику в Interactive и View
5. Обновить документацию в JSON_GUIDE.md

### При изменении структуры референсных значений

1. Обновить `ReferenceRanges` type
2. Обновить `getApplicableRange()` логику
3. Обновить документацию
4. Добавить миграционную логику

---

## 🐛 Частые проблемы

### JSON не парсится

- Проверьте тип параметра: должен быть UPPERCASE (`"NUMBER"`, не `"number"`)
- Используйте `normalizeAnalysisTemplate()` для валидации

### Неправильный статус значения

- Проверьте, что референсные значения указаны
- Убедитесь, что передан пол и возраст пациента в `AnalysisFormView`

### Референсный диапазон не применяется

- Проверьте имена групп: `men`, `women`, `children`, `default`
- Всегда добавляйте `default` как fallback

### Старый формат не мигрирует

- Используйте `normalizeAnalysisTemplate()` перед использованием
- Проверьте, что старые данные в правильном формате

---

## 📊 Примеры референсных значений

### Общий анализ крови

```json
{
  "hemoglobin": {
    "men": { "min": 130, "max": 170 },
    "women": { "min": 120, "max": 150 },
    "children": { "min": 110, "max": 140 }
  },
  "wbc": {
    "default": { "min": 4.0, "max": 9.0 }
  }
}
```

### Биохимия

```json
{
  "glucose": {
    "default": { "min": 3.3, "max": 5.5 }
  },
  "alt": {
    "men": { "max": 41 },
    "women": { "max": 33 }
  }
}
```

---

## 🔬 Медицинские стандарты

При создании шаблонов используйте актуальные референсные значения:

- Международные стандарты (если применимо)
- Локальные нормативы вашей лаборатории
- Учитывайте единицы измерения

---

## 📧 Поддержка

При возникновении вопросов:

1. Проверьте эту документацию
2. Смотрите примеры в [USAGE_EXAMPLE.md](./USAGE_EXAMPLE.md)
3. Изучите примеры JSON в [JSON_GUIDE.md](./JSON_GUIDE.md)
4. Проверьте существующие шаблоны в БД
